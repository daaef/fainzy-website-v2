#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run full build before push
echo "🏗️  Running full build check before push..."
npm run build || { echo "❌ Build failed. Fix errors before pushing."; exit 1; }

echo "✅ Build successful! Proceeding with push..."

